[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "implied-vol-surface"
version = "0.1.0"
description = "Deribit-backed crypto-options market data and analytics workspace."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
include = ["iv_surface*"]
